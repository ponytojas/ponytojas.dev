<section class="hero" id="top">
  <div class="shell hero-shell">
    <div class="hero-intro">
      <p>Senior software engineer</p>
      <p>Spain · <span data-local-time>--:--</span> CET</p>
      <p class="hero-intro-right">Product-minded<br />systems builder</p>
    </div>

    <div class="hero-type" aria-label="Dani Villalobos">
      <span class="hero-name hero-name-one">DANI</span>
      <span class="hero-name hero-name-two">VILLALOBOS</span>

      <div class="hero-orbit" aria-hidden="true">
        <div class="orbit-line orbit-line-one"></div>
        <div class="orbit-line orbit-line-two"></div>
        <div class="orbit-core">
          <span>BUILD</span>
          <span>USE</span>
          <span>LEARN</span>
        </div>
        <span class="orbit-moon"></span>
      </div>
    </div>

    <div class="hero-bottom">
      <p class="hero-statement">
        I build software that holds up in the real world—<span class="serif-accent">clear, useful, resilient.</span>
      </p>
      <div class="hero-actions">
        <a class="hero-cv" href="/cv-danielVillalobos.pdf" target="_blank" rel="noreferrer">
          View CV <span aria-hidden="true">↗</span>
        </a>
        <a class="hero-scroll" href="#work">
          <span>Scroll to explore</span>
          <svg viewBox="0 0 30 30" aria-hidden="true">
            <path d="M15 3v22M7 17l8 8 8-8" fill="none" stroke="currentColor" stroke-width="1.5"></path>
          </svg>
        </a>
      </div>
    </div>
  </div>

  <div class="ticker" aria-hidden="true">
    <div class="ticker-track">
      <span>Distributed systems</span><i>✦</i><span>Product engineering</span><i>✦</i><span>Geospatial data</span><i>✦</i><span>Useful interfaces</span><i>✦</i>
      <span>Distributed systems</span><i>✦</i><span>Product engineering</span><i>✦</i><span>Geospatial data</span><i>✦</i><span>Useful interfaces</span><i>✦</i>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    overflow: hidden;
    background: var(--cream);
  }

  .hero-shell {
    display: flex;
    min-height: calc(100svh - 3.25rem);
    padding-top: 7rem;
    padding-bottom: clamp(2rem, 4vw, 4rem);
    flex-direction: column;
  }

  .hero-intro {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 1rem;
    font-size: 0.66rem;
    line-height: 1.5;
    text-transform: uppercase;
  }

  .hero-intro p {
    margin: 0;
  }

  .hero-intro p:nth-child(2) {
    text-align: center;
  }

  .hero-intro-right {
    text-align: right;
  }

  .hero-type {
    position: relative;
    z-index: 1;
    display: grid;
    min-height: clamp(21rem, 53vw, 42rem);
    margin-top: clamp(1.8rem, 4vw, 4rem);
    place-content: center;
  }

  .hero-name {
    position: relative;
    z-index: 2;
    display: block;
    font-family: var(--font-display);
    font-size: clamp(5.4rem, 13.8vw, 14.2rem);
    font-weight: 900;
    letter-spacing: -0.09em;
    line-height: 0.7;
    white-space: nowrap;
    text-shadow: 0 0.035em 0.055em rgba(23, 23, 20, 0.09);
    animation: name-enter 900ms 120ms cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  .hero-name-one {
    transform: translateX(-10vw);
  }

  .hero-name-two {
    transform: translateX(2vw);
    animation-delay: 210ms;
  }

  .hero-orbit {
    position: absolute;
    z-index: 1;
    top: 38%;
    left: 50%;
    width: clamp(16rem, 34vw, 33rem);
    aspect-ratio: 1;
    border-radius: 50%;
    transform: translate(-50%, -50%);
    animation: orbit-enter 900ms 280ms cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  .orbit-line {
    position: absolute;
    inset: 0;
    border: 1px solid var(--ink);
    border-radius: 50%;
  }

  .orbit-line-one {
    transform: rotate(18deg) scaleY(0.34);
  }

  .orbit-line-two {
    transform: rotate(-46deg) scaleY(0.25);
  }

  .orbit-core {
    position: absolute;
    inset: 12%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    border: 1px solid var(--ink);
    border-radius: 50%;
    background: var(--orange);
    color: var(--cream);
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 6vw, 6rem);
    font-weight: 900;
    letter-spacing: -0.08em;
    line-height: 0.72;
    transform: rotate(-8deg);
    animation: orbit-float 6s ease-in-out infinite;
  }

  .orbit-moon {
    position: absolute;
    top: 3%;
    right: 4%;
    width: 12%;
    aspect-ratio: 1;
    border: 1px solid var(--ink);
    border-radius: 50%;
    background: var(--acid);
    animation: moon-float 5s ease-in-out infinite reverse;
  }

  .hero-bottom {
    position: relative;
    z-index: 4;
    display: grid;
    align-items: end;
    grid-template-columns: minmax(0, 2fr) 1fr;
    gap: 2rem;
    margin-top: auto;
  }

  .hero-statement {
    max-width: 29ch;
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.4rem, 2.6vw, 2.7rem);
    font-weight: 700;
    letter-spacing: -0.04em;
    line-height: 1;
  }

  .hero-statement .serif-accent {
    color: var(--orange);
    font-size: 1.12em;
    font-style: italic;
  }

  .hero-scroll {
    display: flex;
    align-items: center;
    justify-self: end;
    gap: 1rem;
    font-size: 0.66rem;
    font-weight: 700;
    text-transform: uppercase;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    justify-self: end;
    gap: clamp(1rem, 2.5vw, 2.5rem);
  }

  .hero-cv {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    border-bottom: 1px solid currentColor;
    padding-bottom: 0.25rem;
    font-size: 0.66rem;
    font-weight: 700;
    text-transform: uppercase;
  }

  .hero-cv span {
    transition: transform 180ms ease;
  }

  .hero-cv:hover span {
    transform: translate(0.18rem, -0.18rem);
  }

  .hero-scroll svg {
    width: 2.8rem;
    height: 2.8rem;
    padding: 0.55rem;
    border: 1px solid currentColor;
    border-radius: 50%;
    transition: background 180ms ease, color 180ms ease;
  }

  .hero-scroll:hover svg {
    background: var(--ink);
    color: var(--cream);
  }

  .ticker {
    height: 3.25rem;
    overflow: hidden;
    border-top: 1px solid var(--ink);
    border-bottom: 1px solid var(--ink);
    background: var(--acid);
  }

  .ticker-track {
    display: flex;
    width: max-content;
    height: 100%;
    align-items: center;
    gap: 1.7rem;
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    animation: ticker 22s linear infinite;
  }

  .ticker-track i {
    font-size: 1.1rem;
    font-style: normal;
  }

  @keyframes orbit-float {
    50% { transform: rotate(-4deg) translateY(-0.8rem); }
  }

  @keyframes moon-float {
    50% { transform: translate(0.5rem, 0.8rem); }
  }

  @keyframes ticker {
    to { transform: translateX(-50%); }
  }

  @keyframes name-enter {
    from {
      opacity: 0;
      translate: 0 3rem;
    }
  }

  @keyframes orbit-enter {
    from {
      opacity: 0;
      transform: translate(-50%, -50%) scale(0.72) rotate(-12deg);
    }
  }

  @media (max-width: 700px) {
    .hero-shell {
      min-height: calc(100svh - 3.25rem);
      padding-top: 6.7rem;
      padding-bottom: 1.6rem;
    }

    .hero-intro {
      grid-template-columns: 1fr 1fr;
    }

    .hero-intro p:nth-child(2) {
      text-align: right;
    }

    .hero-intro-right {
      display: none;
    }

    .hero-type {
      display: flex;
      min-height: 25rem;
      margin-top: 1rem;
      justify-content: center;
      flex-direction: column;
    }

    .hero-name {
      width: 100%;
      font-size: clamp(3.7rem, 15.5vw, 5.5rem);
      line-height: 0.78;
      text-align: center;
    }

    .hero-name-one {
      transform: translateX(-15vw);
    }

    .hero-name-two {
      transform: translateX(0);
    }

    .hero-orbit {
      top: 48%;
      width: min(69vw, 21rem);
    }

    .orbit-core {
      font-size: clamp(2.4rem, 12vw, 4rem);
    }

    .orbit-core span {
      display: none;
    }

    .hero-bottom {
      display: block;
    }

    .hero-statement {
      font-size: 1.55rem;
    }

    .hero-actions {
      justify-content: flex-start;
      margin-top: 1.2rem;
    }

    .hero-scroll {
      display: none;
    }
  }
</style>
